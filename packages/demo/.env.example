# ePDS Demo — Environment Variables
# Copy to .env.local and adjust for your setup.

# Public URL of this demo app (used for OAuth client_id and redirect_uri)
PUBLIC_URL=http://127.0.0.1:3002

# PDS URL (default matches docker-compose PDS_HOSTNAME=pds.example via Caddy)
PDS_URL=https://pds.example

# Internal PDS URL for server-to-server calls (PAR, token exchange).
# In Docker, the public PDS_URL may not be reachable due to hairpin NAT,
# so use the Docker internal network address instead.
# PDS_INTERNAL_URL=http://core:3000

# Auth endpoint (default matches docker-compose auth service via Caddy)
AUTH_ENDPOINT=https://auth.pds.example/oauth/authorize

# For local dev without Caddy (direct to containers):
# PDS_URL=http://localhost:3000
# AUTH_ENDPOINT=http://localhost:3001/oauth/authorize

# PLC directory for DID resolution
# PLC_DIRECTORY_URL=https://plc.directory

# Session signing secret (generate with: openssl rand -base64 32)
# SESSION_SECRET=change-me-in-production
